<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AnadoluCeng.Views.LoginPage">

    <Grid BackgroundColor="Black" Opacity="0.8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackLayout Grid.Row="0" Grid.Column="0" VerticalOptions="CenterAndExpand">
            <Image Source="lock_icon.png" Margin="20,20"></Image>
        </StackLayout>

        <StackLayout Grid.Row="0" Grid.Column="1"  VerticalOptions="CenterAndExpand" Margin="5">
            <Entry x:Name="Entry_Username" Placeholder="Username" BackgroundColor="Gray" Margin="5"/>
            <Entry x:Name="Entry_Password" Placeholder="Password" IsPassword="True" BackgroundColor="Gray" Margin="5"/>
            <Button x:Name="Btn_Signin" Text="Sign in" Margin="5" HeightRequest="50" BackgroundColor="Turquoise" Clicked="SignIn"/>
        </StackLayout>

    </Grid>

</ContentPage>